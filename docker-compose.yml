name: risuai

services:
  risuai:
    container_name: risuai
    build: .
    restart: always
    environment:
      - PORT=6001 
      - NODE_OPTIONS=--max-old-space-size=4096
    ports:
      - 6001:6001
    volumes:
      - /volume2/docker/risusave:/app/save
